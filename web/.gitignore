node_modules

# Output
/.svelte-kit
/build

# pwag
static/pwa

# Env
*.local

src/lib/deployments.ts

.yalc
yalc.lock
# Playwright
test-results
playwright-report/
blob-report/
.playwright

# assets (assetpack pipeline output). All generated by vite.assetpack.ts:
# nothing here is authored, and `pnpm build` or `pnpm dev` recreates all of it.
# `dist` is assetpack's DEFAULT output and is not used any more, but it is
# listed so a stale one cannot be committed again if a version changes the
# default back.
static/assets
dist
.assetpack
src/lib/manifest.json
