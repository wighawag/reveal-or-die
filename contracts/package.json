{
	"name": "template-commit-reveal-contracts",
	"version": "0.0.0",
	"type": "module",
	"exports": {
		".": {
			"types": "./dist/js/index.d.ts",
			"default": "./dist/js/index.js"
		},
		"./deploy/*": "./dist/deploy/*",
		"./rocketh/*": "./dist/rocketh/*",
		"./artifacts/*": "./dist/generated/artifacts/*",
		"./abis/*": "./dist/generated/abis/*",
		"./deployments/*": "./deployments/*",
		"./src/*": "./src/*"
	},
	"files": [
		"dist",
		"src",
		"deployments"
	],
	"dependencies": {
		"@etherplay/delegation": "^0.1.1"
	},
	"devDependencies": {
		"@nomicfoundation/hardhat-keystore": "^3.0.13",
		"@nomicfoundation/hardhat-network-helpers": "^3.0.11",
		"@nomicfoundation/hardhat-node-test-runner": "^3.1.0",
		"@nomicfoundation/hardhat-viem": "^3.0.9",
		"@openzeppelin/contracts": "5.4.0",
		"@rocketh/deploy": "^0.19.17",
		"@rocketh/doc": "^0.19.21",
		"@rocketh/export": "^0.19.23",
		"@rocketh/node": "^0.19.21",
		"@rocketh/proxy": "^0.19.23",
		"@rocketh/read-execute": "^0.19.12",
		"@rocketh/router": "^0.19.19",
		"@rocketh/signer": "^0.19.12",
		"@rocketh/verifier": "^0.19.22",
		"@rocketh/viem": "^0.19.12",
		"@types/node": "^26.2.0",
		"abitype": "^1.3.0",
		"as-soon": "^0.1.6",
		"earl": "^2.0.0",
		"expand-vars": "^0.1.0",
		"faucet-server": "^0.2.0",
		"forge-std": "github:foundry-rs/forge-std#v1.9.4",
		"hardhat": "^3.13.0",
		"hardhat-deploy": "^2.0.24",
		"hardhat-external-artifacts": "^0.0.6",
		"ldenv": "^0.6.0",
		"prettier": "^3.9.6",
		"prettier-plugin-solidity": "^2.4.1",
		"purgatory": "^0.0.8",
		"rocketh": "^0.19.19",
		"set-defaults": "^0.0.5",
		"slippy": "^0.3.1",
		"solidity-kit": "^0.5.7",
		"tsx": "^4.23.12",
		"typescript": "~6.0.3",
		"viem": "^2.55.19",
		"wait-on": "^9.1.0",
		"zellij-launcher": "^0.0.1"
	},
	"scripts": {
		"_prepare": "`tsc` is part of prepare because `dist` is a BUILD INPUT for the web workspace, not just a publish artifact: web/src/lib/offline.ts imports this package's deploy scripts and rocketh config to run them in the browser, and every one of those export paths resolves into dist. Without it a fresh clone or worktree fails `web check` with module-resolution errors that say nothing about this package. It is `tsc` and NOT `pnpm typescript`, which also type-checks test/js: an install must not be gated on a suite's types, and a descendant branch of this template whose own tests do not type-check yet would otherwise be unable to run `pnpm i` at all. Building dist is what an install owes; checking the tests is what `pnpm typescript` is for.",
		"prepare": "set-defaults .vscode && pnpm compile && tsc",
		"---------------------- INTERNAL ----------------------": "",
		":deploy:dev+export": "expand-vars pnpm hardhat --network $MODE deploy --no-compile --skip-prompts && expand-vars rocketh-export -e $MODE",
		"---------------------- DEV ---------------------- ": "",
		"node:local": "ldenv -d localhost hardhat --network local node",
		"node:purgatory": "purgatory --port 8545 --reset-db --db purgatory.db",
		"faucet": "ldenv -w faucet --no-captcha --port 34010",
		"compile": "hardhat compile",
		"compile:watch": "as-soon -w src pnpm compile",
		"fork:execute": "ldenv tsx @=HARDHAT_FORK=@@MODE @@",
		"fork:deploy": "ldenv pnpm @=HARDHAT_FORK=@@MODE compile --build-profile production ~~ pnpm hardhat --network fork deploy --report-gas-used ~~ @@",
		"deploy:dev": "ldenv -d localhost pnpm :deploy:dev+export @@",
		"deploy:watch": "wait-on ./generated && ldenv -m localhost pnpm as-soon -w generated -w deploy pnpm run deploy:dev @@MODE @@",
		"test": "hardhat --network test test",
		"test:watch": "wait-on ./generated && as-soon -w generated -w test hardhat --network test test --no-compile",
		"typescript:watch": "as-soon -w js pnpm typescript",
		"format:check": "prettier --check .",
		"format": "prettier --write .",
		"_lint": "test/solidity is not listed: this repo has no Solidity tests left (delegation's went into @etherplay/delegation), and slippy fails on a pattern that matches nothing. hardhat.config still points at the directory, so put the glob back with the first .t.sol that lands there.",
		"lint": "slippy \"src/**/*.sol\"",
		"---------------------- PRODUCTION ---------------------- ": "",
		"docgen": "ldenv -m default pnpm run deploy @@MODE --save-deployments true --skip-prompts ~~ pnpm rocketh-doc -e @@MODE --except-suffix _Implementation,_Proxy,_Router,_Route ~~ @@",
		"execute": "ldenv -n HARDHAT_NETWORK tsx @@",
		"deploy": "pnpm compile --build-profile production && ldenv hardhat --network @@MODE deploy @@",
		"verify": "ldenv rocketh-verify -e @@MODE @@",
		"export": "ldenv rocketh-export -e @@MODE @@",
		"typescript": "tsc && tsc -p test/js",
		"---------------------- START ---------------------- ": "",
		"stop": "zellij kill-session $npm_package_name",
		"start": "zellij-launcher a $npm_package_name || zellij -n zellij.kdl -s $npm_package_name"
	}
}
