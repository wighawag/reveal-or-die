# Files this repo deliberately does NOT carry from its template.
#
# WHAT THIS IS FOR. Every repo in this tree merges from a `stem` remote. A file
# that a descendant has deleted is not a settled decision as far as git is
# concerned: the next time the template TOUCHES that file, the cascade stops
# with a modify/delete conflict, and whoever is running it has to work out from
# scratch whether the deletion was deliberate. That has already happened
# repeatedly for `sending-indicator.e2e.ts` in bleeps and mandalas, and the only
# record of the intent was a commit message from months earlier.
#
# Listing the path here does not change what git does - the conflict still
# happens - but it makes the answer a lookup instead of an excavation, and
# `scripts/apply-omissions.sh` turns the resolution into one command.
# `web/test/offshoot-omissions.test.ts` fails if any listed path comes back, so
# a cascade that silently reintroduces one cannot be committed quietly.
#
# FORMAT. One path per line, relative to the repo root. `#` starts a comment.
# Give every entry a reason: the next person's question is never "which file",
# it is "why", and an unexplained list is how a deletion outlives the reason for
# it and becomes cargo cult. A DIRECTORY entry covers everything under it, which
# is usually what you want - a whole replaced directory rather than forty files -
# but name one only when the whole directory is what you replaced, because the
# entry also asserts that nothing under it exists.
#
# AND IT IS CHECKED NOW, which is the only reason to trust it. Three guards, all
# pointing different ways:
#
#   web/test/offshoot-omissions.test.ts   every LISTED path is still absent
#   scripts/check-omissions.mjs           every ABSENT path is listed (needs the stem)
#   scripts/apply-omissions.sh            re-drop them all, and run both checks
#
# ENTRIES ARE STEM-RELATIVE, which is the one thing to understand before editing.
# What belongs here is what THIS repo drops from ITS stem, `template-commit-reveal`
# `with/all`. Upstream's list names paths IT drops from jolly-roger (a greetings
# demo, a context file, some suites) and those are already absent from `with/all`,
# so they are not in our stem, cannot be omissions here, and are deliberately not
# copied down. `scripts/check-omissions.mjs` is what makes that unarguable: it only
# ever reports a path that is present in the stem and absent here.
#
# ---------------------------------------------------------------------------
# THE MACHINERY LIVES AT `template-svelte`, THE ROOT OF THIS TREE, and cascades
# down from there, so a change to it belongs there and not here. It was written
# twice before that, once on each branch of the tree, and the two copies had
# diverged into two variants of `check-omissions.mjs` within the week -
# identical in behaviour, nineteen lines apart in what they claimed. An ancestor
# is the distribution point for shared machinery even when it has nothing of its
# own to guard.
#
# `scripts/apply-omissions.sh` IS THE ONE EXCEPTION, AND IT BELONGS TO THIS
# BRANCH OF THE TREE: `template-commit-reveal` and `reveal-or-die` carry a
# version that also runs `scripts/check-dangling-imports.mjs` and gates "now
# commit the merge" behind it, because two cascades out of that template broke
# `reveal-or-die` through hunks that merged CLEANLY. That difference conflicts
# with the root's copy on every cascade that touches the file, for ever, and the
# resolution is always to keep BOTH halves. The script says so at the top of
# itself, on both sides.
#
# THIS LINE USED TO SAY "this template omits nothing - it is where the files come
# from", AND IN THIS REPO IT WAS FALSE TWICE OVER: this is a GAME, not a template,
# and the four entries directly below it are things it omits. It arrived by
# inheritance from template-commit-reveal, where it was also false, and sat here
# above the list contradicting it - which is the defect
# `an-inherited-paragraph-cannot-say-this-repo` names, in the file whose whole job
# is to be read during a merge. Upstream has its own corrected version now,
# naming ITS stem; this is ours, naming ours.
#
# Files here come from `template-commit-reveal`, and what this repo omits is
# THE TEMPLATE'S GAME: its e2e suites, its fixture, and every unit test it writes
# for `$lib/placement`. Each is listed below with what replaced it.
#
# UPSTREAM'S OWN TWO ENTRIES ARE DELIBERATELY NOT TAKEN. It lists
# `web/src/routes/demo` and `web/src/routes/offline-demo`, which are jolly-roger's
# app demo - and our stem does not have them either, so they can never arrive from
# it. An entry for a path the stem does not carry is an assertion that passes
# forever while meaning nothing, which is the state this list must not drift into.
# Take an entry from upstream only when the path exists in OUR stem.

web/e2e/tests/game.e2e.ts
# The PARENT TEMPLATE'S GAME, not this one. It plans a placement on a CELL,
# bonds a stake on that cell, and asserts the cell's `totalStake` moved. This
# game has no cells and no per-cell stake: avatars occupy positions, a plan is a
# sequence of enter/move/exit, and THE BOND IS THE AVATAR - one held in the
# contract's custody.
#
# Its second test asserted what a missed reveal COST. Nothing here charges for
# one and nothing is going to: `MissedRevealState` is Unknown | Clear | Blocked
# | Acknowledging | Failed, and credits are not a stake - they are abstracted
# GAS, the signer's balance denominated in actions, so there is no cost to
# report the way the template forfeits a bonded reserve. A missed reveal here
# blocks play until it is acknowledged, and that is the whole of it.
#
# REPLACED BY web/e2e/tests/board.e2e.ts, which plays the same three beats
# (plan, commit, reveal) against this game: an avatar enters the world, the
# commitment hides where until the reveal, and only the reveal puts it on the
# board. Written on its own path rather than over this one, so the template can
# go on changing its own suite without ever conflicting with ours.

web/e2e/tests/out-of-gas.e2e.ts
# Its SUBJECT belongs here - the signer running dry mid-round, the message that
# names it, the top-up remedy, the resume - but it can only reach that subject
# through a commit, and the commit it makes is the template's (a stake bonded on
# a cell).
#
# REPLACED BY web/e2e/tests/signer-out-of-gas.e2e.ts, which is the same test
# with the same assertions, reaching the same subject through this game's own
# commit. Only the way in had to change.

web/e2e/fixtures/game.ts
# The fixture those two suites drove, and nothing else uses it. Mostly the
# template's model: `stakeOnCell` reads a per-cell `totalStake`, and `roundStep`
# was shaped around a token reserve and a board of cells.
#
# It also held a `stakeAnAvatar` written against THIS game (buy an avatar
# through the setup gate, then wait for it to be in custody). That walk is
# deleted with the rest ON PURPOSE: it was never once executed green, because
# the suite calling it failed earlier on `view.cells`, and an unverified helper
# that reads as authoritative is exactly the thing this tree keeps having to
# delete. The route it maps is worth keeping, so here it is in prose, to be
# re-derived and PROVEN when the suite is written:
#
#   the HUD's setup gate offers `buy` until the account holds an avatar;
#   `purchase.buy()` may raise a payer chooser (`purchase-payment-methods`,
#   `purchase-pay-with-<id>`), then a consent step whose button reads
#   "Sign and buy" or "Buy"; custody is then visible as
#   `context.game.deposited` going from Loaded/[] to Loaded/[one].
#
# REPLACED BY web/e2e/fixtures/board.ts, which reads this game's board (round
# step, planned actions, where the avatar is, what is in custody) and drives it
# (buy an avatar, skip the welcome overlay, plan by clicking). `stakeAnAvatar`
# was re-derived there and is now executed green by both suites, which is what
# it never was here.
#
# The old code, if it is ever wanted, is in e0b7885.

web/test/lib/placement
# THE TEMPLATE'S GAME AGAIN, this time as a unit test, and it arrived on
# 2026-09-24 as a CLEAN ADD into a directory this repo does not otherwise have.
# It tests `$lib/placement/advance.ts` - `getAttendance` read and `advanceCycle`
# sent against the template's contracts. This game's half of the same job is
# `web/src/lib/world/advance.ts`, against `getCycleNumber` and `moveToNextPhase`, and
# it has tests of its own beside it.
#
# LISTED AS THE WHOLE DIRECTORY, not just `advance.test.ts`, because it is the
# fourth of its kind and there will be a fifth. It used to be the single path
# while this paragraph claimed the class - prose and data disagreeing, so the
# other ten files in there were unlisted and `check-omissions.mjs` reported every
# one of them. The entry is the directory now. `web/src/lib/placement/` is the template's
# game and this repo's is `web/src/lib/world/`; every test the template writes
# under `web/test/lib/placement/` is therefore a test of code that does not
# exist here, and it arrives with no conflict because git has nothing to
# conflict it against. Add the next one here rather than working out afresh
# whether it belongs.
#
# `node scripts/check-dangling-imports.mjs` is what finds them, and it is
# already wired into scripts/apply-omissions.sh, so resolving a cascade the
# documented way reports them before the commit rather than after the build.

# ===========================================================================
# THE FORTY-EIGHT BELOW were all missing until 2026-09-25, when the list was
# checked against the stem for the first time and held 4 of 52. Nothing was
# broken by their absence; what was missing was the ANSWER to "was this
# deliberate?", at the only moment anyone asks - mid-merge. See the finding of
# that date on the `work` branch.

# ---------------------------------------------------------------------------
# THE TEMPLATE'S GAME. This is the big one, and it is the whole point of the
# split: `web/src/lib/placement/` IS template-commit-reveal's reference game
# (cells, a bonded ERC20 stake, a per-cell tally) and this repo's game is
# `web/src/lib/world/` (a maze, avatars, enter/move/exit, custody as the bond).
# The template is arranged by DIRECTORY precisely so that "what do I replace" has
# an obvious answer, and this is that answer being given.
#
# LISTED AS THE DIRECTORY, so the template can keep developing its own game
# without a single conflict here, and so the next file it adds under it is covered
# in advance rather than arriving as a clean ADD nobody notices. That is not
# hypothetical: five such files arrived here in one 2026-09-23 cascade carrying
# nine imports of `$lib/placement`, all clean, and `check-dangling-imports.mjs`
# was written because of it.
web/src/lib/placement

# ---------------------------------------------------------------------------
# THE TEMPLATE'S CONTRACTS, which are not inherited in this tree at all - each
# game carries its own (AGENTS.md). Two groups, and they differ for one reason
# worth stating: WHAT IS AT STAKE.
#
# The template bonds a freely-mintable ERC20 and sells it through `StakeSale`;
# here the stake is the AVATAR, held in the game's custody, and there is no token
# at all. So `contracts/src/tokens/` has no counterpart to keep - our equivalents
# are `contracts/src/avatars/` (Avatars, AvatarsSale, Locker and the two payment
# routes) plus the `GameDeposit` route that takes custody.
contracts/src/tokens

# The identity half of the template's `with/nft-identity` branch. Same subject as
# ours - an account owns a token and the token plays - and a different
# implementation: ours is `contracts/src/avatars/` with `GameDeposit.sol`, and our
# `GameIdentity` is a bigint declared in `web/src/lib/game/identity.ts`.
contracts/src/game/avatar

# The deploys for all of the above. Ours are `001_deploy_avatars.ts`,
# `010_deploy_game.ts` and `020_deploy_sale.ts`: no token to deploy, and the sale
# sells avatars rather than stake.
contracts/deploy/001_deploy_token.ts
contracts/deploy/005_deploy_avatars.ts
contracts/deploy/020_deploy_stake_sale.ts

# The template's own contract suites. Each one is written against the stake model
# above - they share a fixture exporting `TURN_BOND` and a `Placement` of
# `{cellID}`, neither of which exists here - so none of them can run against these
# contracts.
#
# WHAT SURVIVES AND WHAT DOES NOT, said plainly rather than implied. The cycle
# policy is covered here by `contracts/test/js/ManualCycle.test.ts`, and entry and
# custody by `Avatars.test.ts` and `Game.test.ts`. THE GAS BUDGET IS NOT COVERED
# HERE, and that is a real gap rather than a replacement: `GasBudget.test.ts`
# pins the worst-case gas of a turn against the credits multiplier, and by
# ADR-0003's rule that figure is per-repo because it is measured against these
# contracts. A suite of our own is owed; it is not this file's to write.
contracts/test/js/CyclePolicy.test.ts
contracts/test/js/GasBudget.test.ts
contracts/test/js/StakeSale.test.ts

# ---------------------------------------------------------------------------
# THE CANVAS-2D HOST'S TEST AND ITS HARNESS. The subject is shared - this repo
# has `web/src/lib/game/render/canvas2d/` and tests it in `renderers.test.ts` -
# but the stem's suite draws THE TEMPLATE'S BOARD: it imports
# `$lib/placement/render/board-immediate` and `$lib/placement/view`, both deleted
# above. Only the thing being drawn had to change, and it did, on its own path.
web/test/lib/game/render/Canvas2DHarness.svelte
web/test/lib/game/render/canvas2d.svelte.test.ts

# ---------------------------------------------------------------------------
# A SUITE FOR A FACTORY THIS REPO DOES NOT HAVE. The stem's
# `web/test/lib/game/identity.test.ts` tests `createActiveIdentity`, exported by
# the stem's `game/identity.ts` to answer "which of this account's avatars is
# playing" off the chain. Ours is heavily diverged and exports no such factory -
# `GameIdentity`, `onchainIdentity` and `ActiveIdentityStore`, and the lookup lives
# elsewhere - so the suite does not merely fail here, it cannot compile.
#
# WORTH KNOWING WHAT IS THEREFORE UNGUARDED, because the honest answer is not
# "covered elsewhere": `web/test/identity-boundary.test.ts` is STRUCTURAL (no
# shared module may name the concrete identity type) and says nothing about
# behaviour. Whatever resolves the active identity here should get its own
# behavioural suite; the stem's three states, the stale list most of all, are the
# ones to steal when it does.
web/test/lib/game/identity.test.ts
